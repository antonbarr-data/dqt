{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "description": "Check run verdicts — stored as slug in DB",
  "values": [
    { "slug": "pass", "label": "Pass",    "color": "pass" },
    { "slug": "warn", "label": "Warning", "color": "warn" },
    { "slug": "fail", "label": "Failure", "color": "fail" }
  ]
}
