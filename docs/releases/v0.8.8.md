# v0.8.8 -- 2026-05-12

## CLI cosmetic polish

- **P.19** `dqt version` now prints Python version and platform alongside the dqt version
- **P.20** Run output colors verdicts: green=pass, yellow=warn, red=fail (rich)
- **P.22** `dqt run --quiet` / `-q` suppresses all output except errors
- **P.28** `GET /health` returns `{status, version, uptime_seconds}`
- **P.17** Replaced stray `print()` calls with structlog logger in library/CLI internals
