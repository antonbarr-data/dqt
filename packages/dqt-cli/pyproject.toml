[project]
name = "dqt-cli"
version = "0.1.0"
description = "dqt command-line interface"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
    "dqtlib>=0.1.0",
    "typer>=0.12",
    "rich>=13.0",
    "pyyaml>=6.0",
    "duckdb>=0.10",
    "anthropic>=0.26",
]

[project.scripts]
dqt = "dqt_cli.main:app"

[tool.uv.sources]
dqtlib = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dqt_cli"]
