[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c '/app/.venv/bin/uvicorn dqt_server.main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[deploy.healthcheck]
path = "/health"
timeout = 30
